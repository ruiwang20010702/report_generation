# ==============================
# Zeabur 环境变量清单模板（批量粘贴可用）
# 说明：将本文件作为参考，在 Zeabur 仪表盘的 Environment Variables 中逐项创建。
# 注意：不要将真实密钥提交到仓库；仅在平台上设置。
# ==============================

# ========== 基础运行 ==========
NODE_ENV=production
PORT=3001 # Zeabur 会自动注入 PORT；本地可保留 3001
USE_MOCK_ANALYSIS=false # 生产环境建议 false；无服务端秘钥时可临时 true
# FRONTEND_URL=https://your-frontend-domain.example.com # 分离部署时设置；单服务同域可不填

# ========== 代理（如需） ==========
# 若在国内网络或需要企业代理，按需启用。不要同时设置 HTTP 与 HTTPS，仅选一。
# HTTPS_PROXY=http://user:pass@host:port
# HTTP_PROXY=http://user:pass@host:port

# ========== AI 模型（主用 GLM，其他可选） ==========
GLM_API_KEY= # 必填（服务端强制使用 GLM 模型）
# OPENAI_API_KEY= # 可选（Whisper/测试用途，形如 sk-...）
# QWEN_API_KEY=   # 可选（通义千问）
# DEEPSEEK_API_KEY= # 可选（DeepSeek）
# ASSEMBLYAI_API_KEY= # 可选（AssemblyAI）

# ========== 阿里云语音（通义听悟/NLS） ==========
ALIYUN_ACCESS_KEY_ID=
ALIYUN_ACCESS_KEY_SECRET=
# ALIYUN_TINGWU_APP_KEY= # 可选（部分听悟 API 需要）
# ALIYUN_NLS_APP_KEY=    # 可选（NLS 语音转写）
TINGWU_LANGUAGE=en # 默认 en；可设 zh-CN 等

# 代理控制（优先使用 ALIYUN_PROXY_URL，其次全局代理）
# ALIYUN_PROXY_URL=http://user:pass@host:port
ALIYUN_ALLOW_PROXY=false
ALIYUN_REJECT_UNAUTHORIZED=true

# ========== 数据库（PostgreSQL，可选） ==========
# 若未使用数据库，可不填。使用外部托管库时，参考提供方连接信息。
# DB_HOST=postgres.example.internal
# DB_PORT=5432
# DB_NAME=app_db
# DB_USER=app_user
# DB_PASSWORD=strong-password-here

# 连接池与超时（有默认值，可按需覆盖）
# DB_POOL_MAX=20
# DB_POOL_IDLE_TIMEOUT=30000
# DB_POOL_CONNECTION_TIMEOUT=10000

# SSL（云上数据库通常建议启用）
# DB_SSL=true
# DB_SSL_REJECT_UNAUTHORIZED=true

# ========== 认证与安全 ==========
JWT_SECRET= # 强烈建议设置为复杂随机值（生产必填）

# ========== 邮件服务（可选） ==========
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-pass
# SMTP_FROM=noreply@example.com

# ========== 前端（如使用 Vite 前端） ==========
# 分离部署：设置为后端公开域名，例如 https://your-backend-service.zeabur.app
# 单服务同域：建议不设置 VITE_API_URL（前端将默认使用 window.location.origin）
# VITE_API_URL=

# ========== 观测与队列（可选，按需启用） ==========
# SENTRY_DSN=
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
# QSTASH_TOKEN=
# ALERT_EMAIL=
# LOG_SERVICE_URL=

# ========== 其他开关（仅测试场景） ==========
# USE_QWEN=false

# ==============================
# 提示：
# 1) 必填：GLM_API_KEY、ALIYUN_ACCESS_KEY_ID、ALIYUN_ACCESS_KEY_SECRET、JWT_SECRET。
# 2) 根据业务启用：数据库与邮件；未使用可留空。
# 3) 所有密钥仅在 Zeabur 仪表盘设置，切勿提交到 Git 仓库。
# ==============================